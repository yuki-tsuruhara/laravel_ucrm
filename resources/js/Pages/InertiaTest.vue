<script setup>
import { Link } from "@inertiajs/inertia-vue3";
import { ref } from "vue";

const newTitle = ref("");
const newContent = ref("");
</script>

<template>
    <div>
        <input type="text" name="newTitle" v-model="newTitle" />{{ newTitle
        }}<br />
        <input type="text" name="newContent" v-model="newContent" />{{
            newContent
        }}<br />
        <Link
            as="button"
            method="post"
            :href="route('inertia.store')"
            :data="{
                title: newTitle,
                content: newContent,
            }"
            >DB 保存テスト</Link
        >
        Inertian test
        <a href="/">a tag</a>
        <Link href="/">Link tag</Link>
        <Link :href="route('inertia.index')">名前付きルート</Link>
        <Link :href="route('inertia.show', { id: 1 })">パラメータのルート</Link>
    </div>
</template>
